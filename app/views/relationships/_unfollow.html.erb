<%= form_with model:current_user, url:user_relationships_path(@user), method: :delete, remote: :true do |f| %>
 <%= f.hidden_field :followed_id, :value => @user.id %>
 <%= f.submit "フォロー中", class: "btn btn-outline-danger btn-sm" %>
<% end %>