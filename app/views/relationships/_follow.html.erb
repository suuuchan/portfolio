<%= form_with model:current_user, url:user_relationships_path(@user), method: :post, remote: :true do |f| %>
 <%= f.hidden_field :follower_id, :value => @user.id %>
 <%= f.submit "フォローする", class: "btn btn-outline-secondary" %>
<% end %>